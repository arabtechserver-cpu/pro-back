# نشر إصلاحات الحماية

التغييرات تُطبّق على الكود وصورة التشغيل. لا تتصل الاختبارات بقاعدة بيانات الموقع أو PayPal أو Telegram.

## إعداد البروكسي

- اضبط `TRUSTED_PROXY_CIDRS` في الخلفية على عناوين بروكسياتك الفعلية فقط، بما فيها حاوية الواجهة إذا كانت تمرر طلبات API. المثال الموجود في `.env.example` فارغ عمدًا؛ القيمة الفارغة تعني تجاهل الهيدرز الممررة. لا تضع عنوان المشرف أو `0.0.0.0/0` أو شبكة خاصة واسعة في هذه القائمة.
- اجعل المدخل العام يعيد إنشاء `X-Forwarded-For` من عنوان الاتصال الموثوق، بدل قبول قيمة يرسلها العميل. الخلفية تتجاهل `CF-Connecting-IP` و`X-Real-IP` وتستخدم نتيجة Express للتحقق من سلسلة البروكسيات من اليمين لليسار.
- امنع الوصول المباشر من الإنترنت إلى منافذ حاويات Next.js والخلفية. الواجهة تمرر `X-Forwarded-For` من المدخل العام؛ السماح بالوصول المباشر لها مع الوثوق بعنوانها في الخلفية يُفسد هذا الحد الأمني.
- إذا كان Cloudflare أمام المدخل العام، اضبط الثقة بعناوين Cloudflare الموثقة في المدخل، ثم أنشئ سلسلة العناوين الصحيحة. لا تثق بهيدر Cloudflare الوارد من اتصال مباشر.
- اختبر أن `/api/ip-access/my-ip` يعرض عنوان المشرف الحقيقي وأن هيدرز مزورة لا تغيره. أعد تسجيل الدخول إذا كانت الجلسة القديمة لا تحتوي `tokenVersion`.

## قاعدة البيانات والتشغيل

- خذ نسخة PostgreSQL كاملة قابلة للاسترجاع قبل النشر؛ تقرير JSON الحالي ليس بديلًا لنسخة قاعدة البيانات كاملة.
- الصورة تنفذ `prisma migrate deploy` ثم تشغّل التطبيق فقط عند النجاح. لا يوجد `migrate resolve --applied` تلقائي، ولا تجاهل لأخطاء الترحيل.
- ترحيل `20260921000000_repair_skipped_security_baseline` يعيد تنفيذ DDL القديم بصورة قابلة للتكرار لإصلاح البيئات التي علّمت الترحيلات كمطبّقة بدون تنفيذها. لا تتغير ملفات الترحيلات السابقة أو checksums الخاصة بها.
- الترحيل التالي يضيف سجل استرجاعات PayPal ويحوّل الاسترجاعات السابقة المسجلة، بدون تعديل أرصدة المستخدمين. بيانات متكررة أو مراجع غير صالحة توقف النشر بدل حذفها تلقائيًا. إذا ظهرت P3005 أو P3009، افحص حالة الترحيل والقاعدة فعليًا؛ لا تتجاوز الخطأ بتعليم ترحيلات لم تُنفذ.
- الخدمة تعمل بمستخدم `node` داخل الحاوية. يجب أن تسمح وحدات التخزين `/app/uploads` و`/app/backups` بالكتابة للمعرّف UID 1000. الصورة تهيئ المجلدات الجديدة بصلاحية 750؛ عدّل ملكية الوحدات القديمة المخصصة لهذه الخدمة عند الحاجة، ولا تستخدم 777.

## PayPal والإيصالات

- اضبط `PAYPAL_WEBHOOK_ID` على معرّف webhook الخاص بنفس حساب PayPal ووضع live/sandbox. اشترك في أحداث `PAYMENT.CAPTURE.COMPLETED` و`PAYMENT.CAPTURE.REFUNDED` و`PAYMENT.CAPTURE.REVERSED`.
- استرجاع المال يربط refund بالـcapture الأصلي، ويُسجّل معرّف المورد والحدث لمنع الخصم المكرر. تحديث المحفظة والسجل يتم في معاملة واحدة بقفل على الدفعة. الأحداث التي تصل قبل تسجيل التحصيل تحصل على 503 ليعيد PayPal إرسالها.
- راجع الاسترجاعات التاريخية التي تجاهلها الإصدار السابق مقابل PayPal؛ لا يمكن استنتاجها أو تعديل أرصدتها تلقائيًا من الكود المحلي. أعد إرسال الأحداث الصحيحة المفقودة بعد مطابقة الدفعات.
- امسح أي نسخ مخزنة من روابط الإيصالات القديمة في CDN عند النشر، بما فيها الروابط المرمّزة إن كانت مخزنة. الكود يمنع الوصول العام الجديد، لكن لا يستطيع إبطال محتوى سبق تخزينه خارج الخادم. افتح الإيصالات فقط عبر مسارات المعاملات التي تتحقق من صاحبها أو صلاحيات الإدارة.
- إرسال OTP للمستخدم أصبح عبر البريد فقط، وكود الإدارة عبر قناة الإدارة المخصصة. السجلات الجديدة لا تحتوي الأكواد أو عناوين الرسائل التي تتضمنها؛ تُطبق سياسة الاحتفاظ المعتادة على السجلات القديمة.

## التحقق المحلي

```sh
npm ci
npm run build
npm test
```

مجموعة الحماية تستخدم PostgreSQL معزولًا في الذاكرة لاختبار SQL والترحيلات، وتختبر المسارات المرمّزة، وإبطال الجلسات، والهيدرز المزورة، واسترجاعات PayPal المكررة والجزئية والتراجع عند فشل قاعدة البيانات، وعدم ظهور OTP في السجلات.
